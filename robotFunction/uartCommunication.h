#include <stdint.h>

// This function converts two character inputs to a single decimal value
// Function: 
//		Takes in two characters and converts them to binary numbers
//    Takes the two binary numbers and concatenates them
//    Takes the concatenated value and turns it into a decimal value
//    Returns the decimal value
// Input: Two characters
// Output: One Decimal Value

int charToDecimal(char coordinate1, char coordinate0);

int singleCharToDecimal(char distance);
